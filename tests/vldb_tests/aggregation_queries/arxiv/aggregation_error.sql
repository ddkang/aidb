SELECT AVG(num_textboxes) FROM arxiv00;
SELECT AVG(num_textboxes) FROM arxiv00;
SELECT AVG(num_textboxes) FROM arxiv00;
SELECT AVG(num_textboxes) FROM arxiv00;
SELECT AVG(num_textboxes) FROM arxiv00;
