SELECT SUM(entity_start) FROM entity00;
SELECT SUM(entity_start) FROM entity00;
SELECT SUM(entity_start) FROM entity00;
SELECT SUM(entity_start) FROM entity00;
SELECT SUM(entity_start) FROM entity00;
