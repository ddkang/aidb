SELECT AVG(entity_start) FROM entity00;
SELECT AVG(entity_start) FROM entity00;
SELECT AVG(entity_start) FROM entity00;
SELECT AVG(entity_start) FROM entity00;
SELECT AVG(entity_start) FROM entity00;
